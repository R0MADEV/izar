LLM_PROVIDER=ollama
OLLAMA_MODEL=llama3.1:8b
OLLAMA_URL=http://localhost:11434
CLAUDE_API_KEY=
OPENAI_API_KEY=

# Email (SMTP send + IMAP read). For Gmail use an app-password: https://myaccount.google.com/apppasswords
# The same SMTP_USER / SMTP_PASS are reused for reading via IMAP.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
